<%
import_script(`
  <script defer>
    if (document.getElementById('hbePass') || document.querySelector('hbe-prefix')) {
      LFluid.utils.waitElementLoaded('hbe-prefix', function() {
        var hbePrefix = document.querySelector('hbe-prefix');
        hbePrefix.parentElement.classList.add('markdown-body');
        LFluid.utils.retry(function() {
          if (LFluid.boot && LFluid.boot.refresh) {
            LFluid.boot.refresh();
            return true;
          }
        }, 100, 10);
      })
    }
  </script>
`)
%>
